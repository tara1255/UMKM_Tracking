import 'package:flutter/material.dart';

class TempatTerdekat extends StatelessWidget {
  static String routeName;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: new Text("Selamat Datang"),
      ),
    );
  }
}
